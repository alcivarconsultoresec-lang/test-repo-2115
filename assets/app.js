const $=(q,ctx=document)=>ctx.querySelector(q);const $$=(q,ctx=document)=>Array.from(ctx.querySelectorAll(q));$$('.filter').forEach(btn=>{btn.addEventListener('click',()=>{$$('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;$$('.portfolio-card').forEach(card=>{card.style.display=f==='all'||card.dataset.category===f?'flex':'none'})})});const form=$('#diagnosticForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form).entries());let score=28;if(d.web==='no')score+=20;if(d.instagram==='activo')score+=12;if(d.whatsapp==='si')score+=12;if(d.leads==='alto')score+=14;if(d.automation==='si')score+=14;score=Math.min(100,score);let need='Landing web comercial + botón WhatsApp';if(score>72)need='Sistema web + embudo WhatsApp + automatización de seguimiento';if(d.automation==='si')need='Automatización comercial con IA + CRM simple + tablero gerencial';$('#scoreValue').textContent=score;$('#diagnosticTitle').textContent='Diagnóstico recomendado';$('#diagnosticText').innerHTML=`<b>${need}</b><br><br>Tu negocio tiene señales suficientes para transformar redes sociales en un sistema medible de captación. La prioridad es ordenar oferta, contacto, seguimiento y prueba visual.`;const msg=encodeURIComponent(`Hola, quiero un diagnóstico para mi negocio. Rubro: ${d.rubro}. Necesidad sugerida: ${need}. Puntaje: ${score}/100.`);$('#diagnosticWa').href=`https://wa.me/56981408975?text=${msg}`})}$$('[data-wa]').forEach(el=>{el.addEventListener('click',()=>{const text=encodeURIComponent(el.getAttribute('data-wa'));window.open(`https://wa.me/56981408975?text=${text}`,'_blank')})});const year=$('#year');if(year)year.textContent=new Date().getFullYear();